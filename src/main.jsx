import React from "react";
import ReactDOM from "react-dom/client";
import ClientTrackerApp from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ClientTrackerApp />
  </React.StrictMode>
);
